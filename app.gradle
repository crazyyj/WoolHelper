// App基础信息引用

ext {
    // 一级 = 关联
    // 二级 [] 中，使用: 关联
    compileSdkVersion = 30
    buildToolsVersion = "30.0.3"
    minSdkVersion = 19
    targetSdkVersion = 30
    versionCode = 1
    versionName = "1.0"
}